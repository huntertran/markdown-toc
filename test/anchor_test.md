<!-- TOC insertAnchor:true -->

<!-- /TOC -->

# First heading

Anchors are refreshed (deleted, then written again) whenever insertAnchor is
on. The option is set in the TOC start marker above so this fixture exercises
that path without touching workspace settings.

<a id="markdown-stale-anchor" name="stale-anchor"></a>

# Second heading

The stale anchor above belongs to a heading that no longer exists. It has to
disappear once the anchors are rewritten.
