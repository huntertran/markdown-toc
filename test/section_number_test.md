<!-- TOC -->

<!-- /TOC -->

# 1. First section

This document already numbers its headers, so `detectAndAutoSetSection` keeps
the numbers in sync on every update. The numbers below are deliberately wrong.

# 5. Second section

A sibling of the first section, so it has to become "2.".

## 9.9. A child section

Nested one level under the section above, so it has to become "2.1.".

## Child without a number

Numbering is applied to every header of a document that numbers any of them.

# Third section

The last top level section, so it has to become "3.".
